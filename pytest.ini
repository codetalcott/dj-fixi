[pytest]
DJANGO_SETTINGS_MODULE = tests.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --reuse-db --nomigrations
markers =
    browser: drives a real Chromium through Playwright (pip install playwright; playwright install chromium)
